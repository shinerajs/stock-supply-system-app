import { NestedobjectsearchPipe } from './nestedobjectsearch.pipe';

describe('NestedobjectsearchPipe', () => {
  it('create an instance', () => {
    const pipe = new NestedobjectsearchPipe();
    expect(pipe).toBeTruthy();
  });
});
